// S.No and Description stay pinned to the left edge of the snag table while
// the rest of the columns scroll underneath them — both snag-table.tsx
// (header) and snag-row.tsx (body cells) apply these so the columns line up.
export const STICKY_SNO_CLASS = "sticky left-0 z-10 w-14 bg-card"
export const STICKY_DESC_CLASS = "sticky left-14 z-10 bg-card"
